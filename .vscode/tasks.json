{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Upload to NXT",
            "type": "shell",
            "command": "pynxc -d -r '${relativeFile}'",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}